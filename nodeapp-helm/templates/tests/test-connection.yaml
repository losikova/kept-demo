apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodeapp-demo.fullname" . }}-test-connection"
  labels:
    {{- include "nodeapp-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodeapp-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
