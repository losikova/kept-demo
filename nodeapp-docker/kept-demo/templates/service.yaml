apiVersion: v1
kind: Service
metadata:
  name: {{ include "kept-demo.fullname" . }}
  labels:
    {{- include "kept-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kept-demo.selectorLabels" . | nindent 4 }}
